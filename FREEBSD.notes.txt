# running the base makefile,
pkg install gmake
mkdir -p out
ln -s /usr/local/bin/python3.9 /usr/local/bin/python3
ln -s /usr/local/bin/python3.9 /usr/local/bin/python
pkg install avr-gcc

# now on to compiler errors,
# unixcarbide@klipper1:~/klipper $ vi scripts/install-
# scripts/install-arch.sh          scripts/install-centos.sh        scripts/install-octopi.sh        scripts/install-ubuntu-22.04.sh 
# scripts/install-beaglebone.sh    scripts/install-debian.sh        scripts/install-ubuntu-18.04.sh 
# unixcarbide@klipper1:~/klipper $ vi scripts/install-debian.sh

# found python2 virtualenv in the install scripts,
#pkg install python2
## python2-2_3 is installed with this
# no clean way to install python pip for 2x

# for later, (not used now)
pkg install py39-virtualenv
pkg install py39-pip

##############################################################################
# Paused, focusing on kiuah
# Based on a conversation in the Klipper discord, it appears:
#  - the install-*.sh scripts are not very much used any more 
#  - the python2 stuff is still kept for reverse compatibility
# Focusing  nowon the portability snags in kiuah that prevent it from
# running on *BSD
